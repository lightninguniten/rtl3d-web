# Node tooling (lint/format only — no bundler, no build output)
node_modules/

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
.venv/
venv/
env/

# Secrets and credentials (never commit)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json
*.credentials
.npmrc

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
.vscode/
.idea/

# Logs and temp
*.log
*.tmp
~$*

# Audio build intermediates (final mix lives at video/narration*.mp3)
tools/_voiceover.wav
video/narration-*.wav
video/_tts_ja_parts/

# Optional: uncomment to shrink the repo (run build-osm-data.bat after clone)
# data/osm/power-infrastructure.json
# data/osm/water-infrastructure.json
